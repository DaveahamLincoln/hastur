<%= form_for(@local_channel) do |f| %>
  <% if @local_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@local_channel.errors.count, "error") %> prohibited this local_channel from being saved:</h2>

      <ul>
      <% @local_channel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
